server:
  port: 8010
  servlet:
    context-path: /manage
    encoding:
      force-response: true
logging:
  level:
    root: info
  file:
    path: log/store
spring:
  application:
    name: manage
  profiles:
    active: develop
