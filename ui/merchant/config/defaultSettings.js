const proSettings = {
  navTheme: 'dark',
  // 拂晓蓝
  primaryColor: '#1890ff',
  layout: 'side',
  contentWidth: 'Fluid',
  fixedHeader: false,
  fixSiderbar: true,
  colorWeak: false,
  pwa: false,
  iconfontUrl: '',
  title: '供应链管理系统',
  //baseUrl: process.env.NODE_ENV === 'production' ? './' : '/',
  //requestPrefix: process.env.NODE_ENV === 'production' ? './scm/' : '/api/',
};
export default proSettings;
